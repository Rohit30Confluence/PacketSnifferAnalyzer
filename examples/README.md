# PacketSnifferAnalyzer — Examples

This directory contains usage examples for PacketSnifferAnalyzer.

| Directory | Description |
|---|---|
| `cli/` | CLI usage examples and scripts |
| `plugins/` | Example plugin implementations |
| `configs/` | Example configuration files |

---

## CLI Examples

See `cli/` for example shell scripts demonstrating common workflows.

## Plugin Examples

See `plugins/` for example plugin implementations. These are more complete
than the built-in example and demonstrate real-world use cases.

## Configuration Examples

See `configs/` for example configuration files.
